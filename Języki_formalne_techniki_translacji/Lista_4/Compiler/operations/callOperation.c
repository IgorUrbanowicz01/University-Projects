
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../codeGen/code.h"
#include "../expr.h"

void call_Operation(struct CodeList *list, struct expr *expr)
{
    switch (expr->kind)
    {
    case EXPR_MUL:

        break;

    default:
        break;
    }
}